/**
 * Take the form submittion data from react-hook-form
 * and turn into Gravity Form consumable JSON.
 * @param  object   dirty    JSON returned by react-hook-form api
 * @return object            Clean Gravity form friendly JSON
 */

export default dirty => {
    return clean
}
